Decomposition of Saving from ArcPRO to a QGIS File format.

Main steps:
    -Get the current project object (arcpy.mp.ArcGisProject("Current"))
    -Get the list of "Map" Objects from project
        -For each "Map" in the project:
            -Get it's Layers, process the layers
    -Get the list of "Layout" Objects from project
        -For each "Layout" object in the project:
            -Get it's
